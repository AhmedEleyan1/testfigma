
  # Prototype Membership Integration (Copy)

  This is a code bundle for Prototype Membership Integration (Copy). The original project is available at https://www.figma.com/design/ZAkdikwYbEZiKqkncSOuqS/Prototype-Membership-Integration--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  